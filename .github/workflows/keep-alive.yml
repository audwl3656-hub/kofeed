name: Keep Streamlit Alive

on:
  schedule:
    - cron: "0 */11 * * *"  # 11시간마다
  workflow_dispatch:         # 수동 실행 가능

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -s -o /dev/null -w "%{http_code}" https://kofeed.streamlit.app/
