name: wake-streamlit

on:
  schedule:
    - cron: '0 */10 * * *'   # 10시간마다 실행

jobs:
  wake:
    runs-on: ubuntu-latest
    steps:
      - name: Ping kofeed.streamlit.app
        run: curl -L --max-time 30 https://kofeed.streamlit.app/
